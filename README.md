# form-validation
form validation using only javascript and contarins validtion API
whick provide a quick check for if data is good or data is in right format
and this is process called client-side validation
